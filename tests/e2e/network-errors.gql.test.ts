// import { badClient } from '../../../config/apolloClient'

// TODO catch network error test
describe('GraphQL Client Network Errors', () => {
    test('catch network error', async () => {})
})

// TODO catch network error inside resolver test
describe('GraphQL resolver network error', () => {
    test('catch network error thrown inside resolver', async () => {})
})

# Migration `watch-20191102144438`

This migration has been generated by velasko at 11/2/2019, 2:44:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "alicialonso-dev"."Event" DROP COLUMN "subtitle",
ADD COLUMN "subtitle" text   ;

ALTER TABLE "alicialonso-dev"."News" DROP COLUMN "subtitle",
ADD COLUMN "subtitle" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191102043613..watch-20191102144438
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 // ###############################################################################################################
 // USER
@@ -387,9 +387,9 @@
     updatedAt               DateTime            @updatedAt
     author                  User                @relation(name: "UserNewses", onDelete: NONE)
     title                   String 
-    subtitle                String
+    subtitle                String?
     body                    String 
     imageURL                String?
     expiration              DateTime
     category                NewsFeedCategories  @default(NEWS)
@@ -404,9 +404,9 @@
     updatedAt               DateTime            @updatedAt
     author                  User                @relation(name: "UserEvents", onDelete: NONE)
     title                   String 
-    subtitle                String
+    subtitle                String?
     organizer               String?
     body                    String 
     imageURL                String?
     date                    DateTime 
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191102144438)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191102144438'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
